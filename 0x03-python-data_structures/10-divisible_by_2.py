#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    lst = []
    size = len(my_list)
    for i in range(size):
        if (my_list[i] % 2 == 0):
            lst.append(True)
        else:
            lst.append(False)
    return (multiples)
